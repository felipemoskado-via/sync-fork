name: Syncronize fork with original

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: TobKed/github-forks-sync-action@master
        with:
          github_token: ${{ secrets.PERSONAL_TOKEN }}
          upstream_repository: origin/sync-fork
          upstream_branch: main
          target_branch: main
          tags: true